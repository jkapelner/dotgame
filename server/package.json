{
  "name": "game-server",
  "type": "module",
  "version": "1.0.0",
  "description": "game server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build && browserify dist/main.js --standalone server > dist/bundle.js"
  },
  "private": true,
  "author": "jordan kapelner",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^17.0.0",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "line-intersect": "^3.0.0"
  }
}
